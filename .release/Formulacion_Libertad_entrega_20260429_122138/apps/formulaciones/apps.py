from django.apps import AppConfig


class FormulacionesConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.formulaciones"
    label = "formulaciones"
    verbose_name = "Formulaciones"
